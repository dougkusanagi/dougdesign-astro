# Publish Or Schedule

Use this skill after audit passes.

## Steps

1. For immediate publication, run `dougseo publish --slug <slug>`.
2. For scheduled publication, run `dougseo schedule --slug <slug> --at <ISO_DATE>`.
3. Add `--commit` and `--push` when the round should update git automatically.
